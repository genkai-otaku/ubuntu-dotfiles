# 確認ダイアログは出さない。git commit / push / PR 作成は hooks.json 経由の
# pr-mode.sh が /pr 中以外 deny する（Grok の always-approve と同じ役割分担）
approval_policy = "never"
sandbox_mode = "danger-full-access"
model_reasoning_effort = "high"

[features]
hooks = true
